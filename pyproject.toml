[project]
name = "kmcpy"
version = "0.1.0"
description = "Kinetic Monte Carlo Simulation using Python (kMCpy)"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "glob2>=0.7",
    "joblib>=1.5.1",
    "numba>=0.61.2",
    "pymatgen>=2025.5.2",
    "pytest>=8.3.5",
    "scikit-learn>=1.6.1",
]
